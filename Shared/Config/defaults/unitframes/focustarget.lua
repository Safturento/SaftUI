local st = SaftUI

st.defaults.unitframes.profiles["**"].focustarget = {
    enable = false,
    width = 100,
    position = {point = 'LEFT', frame = 'SaftUI_Focus', rel_point = 'RIGHT', x_off = 7, y_off = 0},
    name = {
        enable = false,
    },
}