local st = SaftUI

st.defaults.unitframes.profiles["**"].pettarget = {
    width = 50,
    enable = false,
    position = {
        point = 'RIGHT',
        frame = 'SaftUI_Pet',
        rel_point = 'LEFT',
        x_off = -7,
        y_off = 0,
    },
    health = {
        text = {
            enable = false,
        },
    },
    power = {
        enable = false,
        text = {
            enable = false,
        },
    },
}